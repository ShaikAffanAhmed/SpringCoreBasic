package Abstraction;

abstract class AbsClass   //A class which is declared as abstract is known as an abstract class.
{
	 //A method which is declared as abstract and does not have implementation is known as an abstract method.	
    abstract void run();    
    
}
